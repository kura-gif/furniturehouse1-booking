# 運用マニュアル
## 家具の家 No.1 予約システム 日常運用ガイド

---

## 📋 目次

1. [日常業務フロー](#日常業務フロー)
2. [予約管理](#予約管理)
3. [コスト管理](#コスト管理)
4. [トラブルシューティング](#トラブルシューティング)
5. [月次・定期メンテナンス](#月次定期メンテナンス)

---

## 日常業務フロー

### 🌅 朝のチェックリスト（9:00 AM）

```bash
□ Vercelダッシュボードでエラーログ確認
  → https://vercel.com/yoichi-kurashimas-projects/furniturehouse1-booking

□ Firebaseコンソールで夜間の予約を確認
  → https://console.firebase.google.com

□ Stripeダッシュボードで決済状況確認
  → https://dashboard.stripe.com

□ 新規予約リクエストへの対応
  - pending ステータスの予約を confirmed に変更
  - ゲストに確認メールを送信

□ 本日のチェックイン/アウトを確認
  - サポーターにタスク割り当て
  - 準備状況を確認

□ メール受信確認
  - 問い合わせへの返信
  - 予約変更・キャンセル依頼への対応
```

### 🌙 夕方のチェックリスト（18:00 PM）

```bash
□ 本日の予約完了状況確認
  - チェックイン完了の確認
  - サポータータスクの完了確認

□ 明日の予約準備
  - チェックイン予定の確認
  - サポーターへのリマインダー送信

□ システム稼働状況確認
  - エラーログのチェック
  - パフォーマンスの確認

□ 予約リクエストへの最終返信
```

---

## 予約管理

### 📝 予約ステータスの遷移

```
ゲスト予約
    ↓
pending（リクエスト受付）
    ↓
confirmed（管理者承認）
    ↓
checked_in（チェックイン完了）
    ↓
checked_out（チェックアウト完了）
    ↓
completed（レビュー完了）

または

cancelled（キャンセル）
refunded（返金済み）
```

### 📝 予約承認フロー

#### 手順1: 新規予約リクエストの確認

```bash
1. Firebaseコンソールにアクセス
   → Firestore Database
   → bookings コレクション
   → status = 'pending' でフィルタ

2. 予約詳細を確認:
   - ゲスト名（guestName）
   - メールアドレス（guestEmail）
   - 電話番号（guestPhone）
   - 宿泊日程（checkInDate, checkOutDate）
   - ゲスト数（guestCount）
   - 合計金額（totalAmount）
   - 決済状況（paymentStatus）
```

#### 手順2: カレンダーで重複確認

```bash
# 既存の予約と重複していないか確認
1. 同じ日程で他の confirmed 予約がないか確認
2. 清掃やメンテナンスのスケジュールと重複していないか確認
```

#### 手順3: 予約を承認

```bash
# Firebaseコンソールで手動更新
1. 該当の予約ドキュメントをクリック
2. status フィールドを 'pending' → 'confirmed' に変更
3. confirmedAt フィールドを追加（現在のタイムスタンプ）
4. updatedAt フィールドを更新

または

# 管理者ダッシュボード（要実装）で承認ボタンをクリック
```

#### 手順4: 確認メールを送信

```bash
件名: 【予約確定】家具の家 No.1 ご予約確認

[ゲスト名] 様

この度は家具の家 No.1をご予約いただき、誠にありがとうございます。
以下の内容でご予約を確定いたしました。

━━━━━━━━━━━━━━━━━━━━
予約詳細
━━━━━━━━━━━━━━━━━━━━
予約番号: [bookingReference]
チェックイン: [checkInDate] 15:00〜
チェックアウト: [checkOutDate] 〜11:00
ゲスト数: [guestCount]名
ご請求金額: ¥[totalAmount]

━━━━━━━━━━━━━━━━━━━━
アクセス情報
━━━━━━━━━━━━━━━━━━━━
住所: 〒XXX-XXXX 東京都...
最寄り駅: ...

詳細な案内は、チェックイン3日前にお送りします。

ご不明な点がございましたら、お気軽にお問い合わせください。

家具の家 No.1 運営チーム
━━━━━━━━━━━━━━━━━━━━
```

### 📝 キャンセル処理フロー

#### キャンセルポリシー
```
30日前まで: 全額返金（100%）
7日前まで: 50%返金
6日前以降: 返金なし
```

#### 手順1: キャンセルリクエストの受付

```bash
1. ゲストからのキャンセル依頼を受信
   - メール
   - 電話
   - Webサイトのキャンセルフォーム

2. 予約情報を確認
   - 予約番号（bookingReference）
   - チェックイン日
   - 決済情報（paymentIntentId）
```

#### 手順2: 返金額の計算

```bash
const daysUntilCheckIn = Math.ceil(
  (checkInDate - new Date()) / (1000 * 60 * 60 * 24)
)

let refundRate = 0
if (daysUntilCheckIn >= 30) {
  refundRate = 1.0 // 100%
} else if (daysUntilCheckIn >= 7) {
  refundRate = 0.5 // 50%
} else {
  refundRate = 0 // 0%
}

const refundAmount = Math.floor(totalAmount * refundRate)
```

#### 手順3: Stripeで返金処理

```bash
1. Stripeダッシュボードにアクセス
   → https://dashboard.stripe.com/payments

2. Payment Intentを検索
   - メタデータから予約番号で検索
   - または決済日・金額から検索

3. 返金を実行
   - 「払い戻し」ボタンをクリック
   - 返金額を入力（全額 or 一部）
   - 理由を選択: 「顧客からのリクエスト」
   - 「払い戻しを確認」

⚠️ 返金は通常5-10営業日でゲストのカードに反映されます
```

#### 手順4: Firestoreで予約ステータス更新

```bash
1. 該当の予約ドキュメントを開く
2. 以下のフィールドを更新:
   - status: 'cancelled'
   - cancelledAt: [現在のタイムスタンプ]
   - refundAmount: [返金額]
   - refundedAt: [返金実行日時]
   - cancellationReason: [理由]
   - updatedAt: [現在のタイムスタンプ]
```

#### 手順5: キャンセル確認メールを送信

```bash
件名: 【キャンセル完了】家具の家 No.1 予約キャンセルのお知らせ

[ゲスト名] 様

ご予約のキャンセルを承りました。

━━━━━━━━━━━━━━━━━━━━
キャンセル詳細
━━━━━━━━━━━━━━━━━━━━
予約番号: [bookingReference]
キャンセル日: [cancelledAt]
返金額: ¥[refundAmount]（元の予約金額の[refundRate]%）

返金は5-10営業日以内にご利用のカードに反映されます。

またのご利用をお待ちしております。

家具の家 No.1 運営チーム
━━━━━━━━━━━━━━━━━━━━
```

### 📝 予約変更フロー

#### 日程変更の場合

```bash
1. 元の予約をキャンセル（返金処理）
2. 新しい日程で予約を作成
3. 差額がある場合:
   - 高い場合: 追加決済をリクエスト
   - 安い場合: 差額を返金

⚠️ 変更手数料の設定を検討
   例: 変更1回まで無料、2回目以降は¥3,000
```

#### ゲスト数変更の場合

```bash
1. 新しいゲスト数で金額を再計算
2. 差額を計算
3. 追加決済 or 返金処理
4. Firestoreで予約を更新:
   - guestCount: [新しい人数]
   - totalAmount: [新しい金額]
   - modifiedAt: [変更日時]
```

---

## コスト管理

### 💰 月間コスト目安

#### Firebase（Blaze プラン - 従量課金）

```bash
想定使用量（月間予約20件の場合）:

■ Firestore
読み取り: 約10,000回 → $0.36
書き込み: 約2,000回 → $0.18
削除: 約100回 → $0.02
ストレージ: 1GB以下 → 無料
小計: 約$0.56（約¥80）

■ Authentication
アクティブユーザー: 50名以下 → 無料

■ Hosting（使用しない場合）: $0

月間合計: 約¥80-200
```

#### Stripe（決済手数料）

```bash
■ 国内カード
手数料率: 3.6%

例: ¥50,000の予約の場合
手数料: ¥1,800

月間20件の場合: 約¥36,000（予約総額¥1,000,000の場合）
```

#### Vercel（Hosting）

```bash
■ Hobby プラン（個人利用）
月額: $0（無料）
制限:
- 100GB 帯域幅/月
- 無制限デプロイ
- 商用利用可

■ Pro プラン（推奨）
月額: $20（約¥3,000）
利点:
- パスワード保護
- 分析機能
- より高い帯域幅
- 優先サポート
```

#### メール送信（Gmail SMTP）

```bash
Gmail: 無料
制限: 1日500通まで

または

SendGrid 無料プラン:
月間100通まで無料
有料プラン: $14.95/月（月間40,000通）
```

#### 総コスト見積もり

```bash
固定費:
- Vercel Pro: ¥3,000/月
- その他ツール: ¥0-1,000/月

変動費:
- Firebase: ¥100-500/月
- Stripe手数料: 売上の3.6%
- メール送信: ¥0-2,000/月

月間合計目安: ¥3,000-7,000 + 売上の3.6%
```

### 💰 コスト削減Tips

#### 1. Firestoreの読み取り回数削減

```typescript
// ❌ 悪い例: 毎回全データを取得
const bookings = await getDocs(collection(db, 'bookings'))

// ✅ 良い例: 必要な範囲のみ
const bookings = await getDocs(
  query(
    collection(db, 'bookings'),
    where('checkInDate', '>=', startDate),
    where('checkInDate', '<=', endDate),
    limit(50)
  )
)

// ✅ キャッシュを活用
const cachedBookings = useBookingsCache()
```

#### 2. 画像最適化

```bash
# WebP形式に変換
npm install sharp

# 画像を圧縮してストレージコストを削減
# 推奨サイズ: 800px幅、品質80%
```

#### 3. 不要なデータの削除

```bash
# 古い予約データのアーカイブ（1年以上前）
# 完了したタスクの削除（3ヶ月以上前）
# テストデータの削除
```

### 💰 予算アラートの設定

#### Firebase予算アラート

```bash
1. Firebaseコンソール
2. プロジェクト設定 → 使用量と請求
3. 予算を設定: ¥1,000/月
4. アラート閾値: 50%, 90%, 100%
5. 通知先メール: 管理者メール
```

#### Stripe支出モニタリング

```bash
Stripeダッシュボード:
→ レポート
→ 手数料レポート

月次で確認:
- 総取引額
- 手数料合計
- 返金額
- 正味収益
```

---

## トラブルシューティング

### 🚨 よくある問題と解決策

#### 問題1: 予約が重複してしまった

**原因**
- 同時に複数のユーザーが予約を作成
- トランザクション処理が未実装

**解決策**
```bash
1. 即座に一方の予約をキャンセル
2. 先に決済が完了した方を優先
3. キャンセルされたゲストに謝罪と代替日程を提案
4. 全額返金
5. トランザクション処理を実装（SECURITY_IMPROVEMENTS.md参照）
```

#### 問題2: 決済は完了したが予約が作成されていない

**原因**
- ネットワークエラー
- Firestore書き込みエラー
- クライアントサイドのタイムアウト

**解決策**
```bash
1. Stripeダッシュボードで決済を確認
   → Payment Intentからメタデータを確認
   → ゲスト情報を特定

2. 手動でFirestoreに予約を作成
   コレクション: bookings
   必須フィールド:
   - guestName, guestEmail, guestPhone
   - checkInDate, checkOutDate
   - guestCount, totalAmount
   - paymentIntentId（Stripeから取得）
   - status: 'confirmed'
   - bookingReference: [新規生成]
   - createdAt, updatedAt

3. ゲストに確認メールを送信
```

#### 問題3: メールが届かない

**原因**
- SMTP認証エラー
- スパムフィルタ
- メールアドレスのタイプミス

**解決策**
```bash
1. Vercelログでエラーを確認
   → Functions → Logs

2. メールアドレスを確認
   - タイプミスがないか
   - 有効なドメインか

3. スパムフォルダを確認するようゲストに依頼

4. 別のメールアドレスを試す

5. SMTP設定を確認
   - EMAIL_USERが正しいか
   - EMAIL_PASSWORDがアプリパスワードか
   - Gmailのセキュリティ設定
```

#### 問題4: サイトが表示されない（500エラー）

**原因**
- サーバーエラー
- 環境変数の設定ミス
- Firebase/Stripeの障害

**解決策**
```bash
1. Vercelダッシュボードでエラーログを確認
   → Deployments → [最新デプロイ] → Logs

2. 環境変数を確認
   → Settings → Environment Variables
   → 全ての必須変数が設定されているか

3. ロールバック
   → Deployments → [前のデプロイ] → Promote to Production

4. 外部サービスのステータスを確認
   - Firebase: https://status.firebase.google.com
   - Stripe: https://status.stripe.com
   - Vercel: https://www.vercel-status.com
```

#### 問題5: カレンダーに空き日が表示されない

**原因**
- カレンダーコレクションの設定ミス
- 既存予約との重複

**解決策**
```bash
1. Firestoreの calendar コレクションを確認
   - isAvailable: true になっているか
   - 日付が正しいか

2. 既存予約を確認
   - 該当日に confirmed の予約がないか

3. キャッシュをクリア
   - ブラウザのキャッシュをクリア
   - Ctrl+Shift+R (Windows) / Cmd+Shift+R (Mac)
```

### 🚨 緊急時の連絡フロー

#### レベル1: 軽微な問題（1営業日以内に対応）
```bash
例: メール送信遅延、画像表示の不具合

対応:
- 問題をメモ
- 次の営業日に修正
- ゲストへの影響が小さい
```

#### レベル2: 中程度の問題（4時間以内に対応）
```bash
例: 予約フォームのエラー、決済の一部失敗

対応:
1. 問題を特定（ログ確認）
2. 一時的な回避策を提供（電話予約など）
3. 4時間以内に修正
4. 影響を受けたゲストに連絡
```

#### レベル3: 重大な問題（即座に対応）
```bash
例: サイト全体が停止、決済が全て失敗、データ損失

対応:
1. 即座に開発者に連絡
2. ステータスページで告知
3. 代替手段を用意（電話予約、メール予約）
4. 1時間以内に復旧またはロールバック
5. 全ユーザーに謝罪と説明
```

#### 緊急連絡先リスト

```bash
システム管理者:
- 名前: [管理者名]
- 電話: [電話番号]
- メール: [メールアドレス]
- LINE: [LINE ID]

開発者:
- 名前: [開発者名]
- 電話: [電話番号]
- メール: [メールアドレス]
- Slack/Discord: [ID]

外部サポート:
- Firebase Support: https://firebase.google.com/support
- Stripe Support: https://support.stripe.com
- Vercel Support: support@vercel.com
```

---

## 月次・定期メンテナンス

### 📅 週次タスク（毎週月曜日）

```bash
□ 先週の予約統計を確認
  - 新規予約数
  - キャンセル数
  - 平均予約単価
  - 稼働率

□ エラーログのレビュー
  - Vercelログ
  - Firebaseログ
  - Sentryエラー（導入時）

□ コスト確認
  - Firebase使用量
  - Stripe手数料
  - Vercel帯域幅

□ カレンダーの更新
  - 向こう3ヶ月の空室設定
  - 休業日の設定
  - 料金調整（繁忙期など）
```

### 📅 月次タスク（毎月1日）

```bash
□ 月次レポート作成
  - 予約件数
  - 売上
  - キャンセル率
  - ゲストレビュー
  - コスト内訳

□ 料金設定の見直し
  - 季節料金の調整
  - クーポンの更新
  - 追加料金の見直し

□ データベースクリーンアップ
  - 古い予約のアーカイブ（1年以上前）
  - 完了したタスクの削除（3ヶ月以上前）
  - テストデータの削除

□ セキュリティチェック
  - npm audit で脆弱性確認
  - 依存関係のアップデート
  - アクセスログのレビュー

□ バックアップの確認
  - Firestoreバックアップが正常か
  - 環境変数のバックアップ確認
```

### 📅 四半期タスク（3ヶ月ごと）

```bash
□ 大規模アップデート
  - Nuxt/Vueのメジャーバージョンアップ
  - Firebaseライブラリの更新
  - Stripeライブラリの更新

□ UX改善
  - ユーザーフィードバックの分析
  - A/Bテストの実施
  - デザインの最適化

□ パフォーマンス最適化
  - Lighthouseスコアの確認
  - バンドルサイズの最適化
  - 画像最適化

□ セキュリティ監査
  - 外部セキュリティ監査（推奨）
  - ペネトレーションテスト
  - Firebaseルールの見直し
```

### 📅 年次タスク（1年ごと）

```bash
□ 契約・ライセンスの更新
  - ドメイン更新
  - SSL証明書（自動更新の確認）
  - 有料サービスの契約更新

□ 規約・ポリシーの見直し
  - 利用規約
  - プライバシーポリシー
  - キャンセルポリシー
  - 返金ポリシー

□ 大規模リファクタリング
  - コードの整理
  - 技術的負債の解消
  - アーキテクチャの見直し

□ 事業レビュー
  - 年間売上
  - 稼働率
  - ゲスト満足度
  - 改善点の洗い出し
```

---

## 📊 KPI（重要指標）の追跡

### 毎日確認すべき指標

```bash
1. 予約転換率
   = (予約完了数 / サイト訪問者数) × 100%
   目標: 3-5%

2. 平均予約単価
   = 総売上 / 予約件数
   目標: ¥50,000-80,000

3. キャンセル率
   = (キャンセル数 / 総予約数) × 100%
   目標: 5%以下
```

### 週次確認すべき指標

```bash
4. 稼働率
   = (予約泊数 / 利用可能泊数) × 100%
   目標: 60-80%

5. リピート率
   = (リピートゲスト数 / 総ゲスト数) × 100%
   目標: 20-30%

6. レビュー評価
   平均評価: 4.5以上 / 5.0
```

### 月次確認すべき指標

```bash
7. 売上成長率
   = ((今月売上 - 先月売上) / 先月売上) × 100%

8. 顧客獲得コスト（CAC）
   = マーケティング費用 / 新規顧客数
   目標: 予約単価の20%以下

9. システムコスト比率
   = システム運用コスト / 総売上
   目標: 5%以下（Stripe手数料除く）
```

---

## 🎯 成功のための運用Tips

### 1. ゲストコミュニケーション

```bash
✅ 予約後24時間以内に確認メール
✅ チェックイン3日前にリマインダー
✅ チェックアウト後にサンキューメール
✅ 7日後にレビュー依頼
✅ 質問には6時間以内に返信
```

### 2. 品質管理

```bash
✅ チェックアウト後の清掃を必ず写真で記録
✅ サポーターのタスク完了を確認
✅ 備品の在庫チェック（月1回）
✅ 設備の点検（四半期に1回）
```

### 3. データドリブンな改善

```bash
✅ 予約データを定期的に分析
✅ 人気の宿泊期間を特定
✅ 価格最適化（需要に応じて調整）
✅ ゲストフィードバックを改善に活用
```

### 4. リスク管理

```bash
✅ バックアップは複数箇所に保存
✅ 重要な変更は必ずテスト環境で確認
✅ 本番デプロイは平日午前中に実施
✅ ロールバック手順を常に確認
```

---

**このマニュアルを参照しながら、安定した宿泊施設運営を実現してください。**

**ご不明な点やサポートが必要な場合は、開発チームまでご連絡ください。**
